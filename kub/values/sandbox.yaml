resources:
  requests:
    cpu: 50m
    memory: 300Mi
  limits:
    memory: 2Gi

hpa:
  minReplicas: 1
  maxReplicas: 2

ingress:
  enabled: true
  type: external
  rules:
  - hosts:
    - examples.sandbox.splitit.com
    paths:
    - /*
    port: https

istio:
  hosts:
  - examples.sandbox.splitit.com