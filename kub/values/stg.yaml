resources:
  requests:
    cpu: 20m
    memory: 200Mi
  limits:
    memory: 1Gi

hpa:
  minReplicas: 2
  maxReplicas: 3

ingress:
  enabled: true
  type: internal
  rules:
  - hosts:
    - examples.stg.splitit.com
    paths:
    - /*
    port: https
